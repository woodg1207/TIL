from collections import deque


def solution(bridge_length, weight, truck_weights):
    answer = 0
    q = deque()
    q.extend(truck_weights)
    b = []
    while q:
        
    return answer


B, W, T = 2, 10, [7, 4, 5, 6]
print(solution(B, W, T))